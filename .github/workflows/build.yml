name: Build

on:
  workflow_dispatch:
  push:
    branches: [master]
    tags: ["v*"]

jobs:
  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Build
        run: |
          python -m pip install --upgrade -r requirements.txt pyinstaller
          python tools/make_icon.py
          python -m PyInstaller --noconfirm --clean --onefile --windowed --name MagnifySnap --icon assets/icon.ico --collect-all customtkinter main.py
      - uses: actions/upload-artifact@v4
        with:
          name: MagnifySnap-windows
          path: dist/MagnifySnap.exe

  linux:
    # старый Ubuntu = совместимость с большим числом дистрибутивов (glibc)
    runs-on: ubuntu-22.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: System deps
        run: sudo apt-get update && sudo apt-get install -y python3-tk
      - name: Build
        run: |
          python -m pip install --upgrade -r requirements.txt pyinstaller
          python tools/make_icon.py
          python -m PyInstaller --noconfirm --clean --onefile --windowed --name magnifysnap --collect-all customtkinter main.py
      - uses: actions/upload-artifact@v4
        with:
          name: MagnifySnap-linux
          path: dist/magnifysnap
